/**
 * Right-side inspector — currently disabled per design feedback.
 * Kept as a no-op so the layout import stays stable; per-page inspectors
 * will be added inline by their pages when needed.
 */
export default function InspectorRail() {
  return null;
}
